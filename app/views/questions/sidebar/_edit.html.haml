.well
  %h3 How to Edit
  %p Please only ask one question.
  %p Please supply some code examples if it helps to understand your problem.
- if related_questions.length > 0
  %h3= t("questions.related")
  %ul
    - related_questions.each do |question|
      %li= link_to question.name, question_path(:id => question.id, :friendly_id => question.friendly_id)
