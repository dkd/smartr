<% title image_tag(@user.avatar.url(:medium), :alt => @user.login, :title => @user.login, :class => "thumbnail pull-left", :style => "margin-right: 6px") + @user.login, "<br>#{@user.questions.count} #{t("question", :count => @user.questions.count).capitalize}, #{@user.answers.count} #{t("answer", :count => @user.answers.count).capitalize}, #{@user.comments.count} #{t("comment", :count => @user.comments.count).capitalize}<br style='clear:both'>".html_safe %>
