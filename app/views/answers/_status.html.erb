<% if current_user && answer.question.user == current_user %>
  
    <% if answer == answer.question.answer %>
      <%= link_to(raw("&nbsp;"), 
                  update_for_toggle_acceptance_question_path(answer.question, 
                                                             :answer_id => answer.id, 
                                                             :mode => :toggle_acceptance), 
                  :method => :put,
                  :remote => true,
                  :class => "accepted") %>
    <% else %>
       <%= link_to(raw("&nbsp;"), 
                   update_for_toggle_acceptance_question_path(answer.question, 
                                                              :answer_id => answer.id, 
                                                              :mode => :toggle_acceptance), 
                   :method => :put,
                   :remote => true,
                   :class => "") %>
    <% end -%>
  
  <% else %>
    <%= image_tag("checked.png") if answer.question.answer == answer %>
  <% end -%>
 